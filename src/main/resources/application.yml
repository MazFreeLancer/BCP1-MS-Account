spring:
  application:
    name: ms-account
  profiles:
    active:
      - dev #(2)
  config:
    import: optional:configserver:http://localhost:9010
  cloud:
    config:
      profile: dev

backend:
  #service:
    #url: http://localhost:8082/dummy-client2/saludo
  eureka:
    customer:
      get-by-id: http://ms-customer/customer/{id}

# CONFIGURACION PERSONAL
#spring:
#  application:
#    name: ms-customer
#  cloud:
#    config:
#      enabled: true
#      uri: http://localhost:8082